class Solution {
public:
    bool checkInclusion(string s1, string s2) {
        // what is an permutation
        // check if char in s1 (hashset) then 
        // s1 = "eeba"
        // s2 = "lecabe"
        [{e, 2}, {b ,1}, {a, 1}]
        l
        e -> [{e, 1}, {b , 1}, {a ,1}]
        ec -> not found
        ca -> 

        // Set([eeba])
        l
        e -> found start win
        ec -> not , reset
        a -> yes
        ab -> yes
        e -> yes count = 3

        /// hashmap, and hashmap for permu
        // hash1 = [{e, 2}, {b ,1}, {a, 1}]

        l -> not in skip
        e -> in creates hash [{e , 1}]
        
         



        
    }
};
